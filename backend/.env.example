PORT=3001
NODE_ENV=development
MYSQL_ROOT_PASSWORD=
MYSQL_PASSWORD=
DATABASE_URL=mysql://faztudo:senha@localhost:3306/faztudo
# Gere, por exemplo, com: openssl rand -base64 48
JWT_SECRET=
CORS_ORIGIN=http://localhost:5173
TRUST_PROXY=false
SEED_ON_START=false
# Opcional: senha aleatoria de pelo menos 32 caracteres para novas contas demo.
# Sem este valor, o seed gera uma senha aleatoria que nao e exibida.
SEED_PASSWORD=
